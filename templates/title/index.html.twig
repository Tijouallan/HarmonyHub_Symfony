{% extends 'base.html.twig' %}

{% block title %}Hello TitleController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>bienvenue sur HarmonyHub! ✅</h1>

     <p>Here is a list of all the title in the database</p>
        <p>Click on a title to see more details</p>
        <div>
            {% for title in titles %}
    <div class="card">
        <div class="card-header">
            {{ title.title }}
        </div>
        <div class="card-body">
            <h5 class="card-title">{{ title.artist }}</h5>
            <p class="card-text">{{ title.tags }}</p>
            <a href="#" class="btn btn-primary">See details</a>
        </div>
    </div>
{% endfor %}

        </div>
</div>
{% endblock %}
